plugins {
    id "java"
    id "com.github.node-gradle.node"
}

task buildWebApp(type: NpmTask) {
    args = ["run", "build"]
}

sourceSets {
    java {
        main {
            resources {
                srcDir(buildWebApp)
            }
        }
    }
}
